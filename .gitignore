.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar
.env
*.log

# Artefato de runtime do Rails: pidfile versionado quebra o proximo boot.
console/tmp/
console/log/

# Rails master keys (console/ is a subfolder; the root rule never applied)
**/config/master.key
**/config/credentials/*.key
